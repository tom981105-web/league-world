{
  "name": "league-world",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "analyze:balance": "npm run build && node scripts/analyze-balance.mjs",
    "build": "tsc -p tsconfig.json && vite build",
    "build:engine": "tsc -p tsconfig.json",
    "dev": "vite",
    "test": "npm run build && node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "lucide-react": "^1.33.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "typescript": "^5.0.0",
    "vite": "^8.2.1"
  }
}
